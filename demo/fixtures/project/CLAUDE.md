# Project rules
- Never modify files under src/generated; they are built from src/schema. Change the schema instead.
- Use the repo logger from src/log.ts, never console.log.
- Keep public functions documented with a one-line comment.
- Never add a new dependency without asking first.
